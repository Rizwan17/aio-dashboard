import Layout from "../src/ui-components/layout";
import TestComponents from "../src/Pages/test-components";

export default () => {
    return (
        <Layout>
            <TestComponents />
        </Layout>
    );
}